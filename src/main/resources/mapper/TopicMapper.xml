<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.limyel.shutbb.dao.TopicDao">
    <sql id="insertFields">(title, content, draft, section_id, user_id)</sql>
    <sql id="retriveFields">
        id, created_at, updated_at, title, content, user_id
    </sql>
    <sql id="allFields">
        id, created_at, updated_at, deleted, title, content, user_id, section_id, draft
    </sql>
    <sql id="allFieldsT">
        t.id, t.created_at, t.updated_at, t.deleted, t.title, t.content, t.user_id, t.section_id, t.draft
    </sql>

    <resultMap id="topic" type="topic">
        <id property="id" column="id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deleted" column="deleted"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="draft" column="draft"/>
        <association property="user" javaType="user" column="user_id">
            <id property="id" column="id"/>
            <result property="username" column="username"/>
            <result property="status" column="status"/>
            <result property="avatar" column="avatar"/>
        </association>
        <association property="section" javaType="section" column="section_id">
            <id property="id" column="id"/>
            <result property="name" column="name"/>
        </association>
    </resultMap>

    <insert id="create" parameterType="topic" keyProperty="id" keyColumn="id">
        insert into topic (title, content, draft, section_id, user_id) values (#{title}, #{content}, #{draft}, #{section.id}, #{user.id})
    </insert>
    
    <select id="retriveById" resultMap="topic">
        select <include refid="allFieldsT"/>, s.id, s.name, u.id, u.username, u.status, u.avatar from topic t, user u, section s where t.id=#{id} and u.id=t.user_id and s.id=t.section_id
    </select>

    <select id="retriveBySection" resultType="com.limyel.shutbb.entity.Topic">
        select <include refid="retriveFields"/> from topic where deleted=false and draft=false and section_id=#{sectionId} order by created_at desc
    </select>

    <select id="retriveBySectionName" resultMap="topic">
        select t.id, t.created_at, t.title, u.id, u.username, u.status, u.avatar from topic t, user u where t.section_id in (select id from section where name=#{sectionName}) and u.id=t.user_id order by created_at desc
    </select>

    <select id="retriveByUser" resultType="com.limyel.shutbb.entity.Topic">
        select <include refid="retriveFields"/> from topic where deleted=false and draft=false and user_id=#{userId};
    </select>

    <select id="retriveDraft" resultType="com.limyel.shutbb.entity.Topic">
        select <include refid="retriveFields"/> from topic where deleted=false and draft=true and user_id=#{userId};
    </select>

    <update id="update" parameterType="com.limyel.shutbb.entity.Topic">
        update topic
        <set>
            <if test="title != null">title=#{title},</if>
            <if test="content != null">content=#{content},</if>
            <if test="content != null">draft=#{idDraft},</if>
            <if test="sectionId != null">section_id=#{sectionId},</if>
            <if test="userId != null">user_id=#{userId},</if>
        </set>
        where id=#{id};
    </update>

    <update id="deleteById">
        update topic set deleted=true where id=#{id} and user_id=#{user.id};
    </update>

    <select id="countBySection" resultType="java.lang.Integer">
        select count(id) from topic where section_id=#{sectionId}
    </select>
</mapper>