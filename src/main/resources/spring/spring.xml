<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--  ignore-unresolvable 不加这个属性，则无法引入多个配置文件  -->
    <context:property-placeholder location="classpath:db.properties" ignore-unresolvable="true"/>
    <context:property-placeholder location="classpath:config.properties" ignore-unresolvable="true"/>
    <context:property-placeholder location="classpath:redis.properties" ignore-unresolvable="true"/>
    <context:property-placeholder location="classpath:email.properties" ignore-unresolvable="true"/>

    <context:component-scan base-package="com.limyel.shutbb.service.impl"/>
    <context:component-scan base-package="com.limyel.shutbb.resolver"/>
    <context:component-scan base-package="com.limyel.shutbb.util"/>

    <import resource="classpath:spring/spring-mybatis.xml"/>
    <import resource="classpath:spring/spring-kaptcha.xml"/>
    <import resource="classpath:spring/spring-redis.xml"/>
    <import resource="classpath:spring/spring-email.xml"/>

    <!--  自己的类  -->
    <bean class="com.limyel.shutbb.service.impl.AuthorizationServiceImpl" id="authorizationService">
        <property name="secret" value="${config.jwt_secret}"/>
    </bean>
    <bean class="com.limyel.shutbb.util.RedisUtil" id="redisUtil">
        <property name="redisTemplate" ref="redisTemplate"/>
    </bean>

</beans>