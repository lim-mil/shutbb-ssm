<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        https://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
    <!--  使用注解驱动  -->
    <mvc:annotation-driven/>
    <mvc:annotation-driven>
        <mvc:argument-resolvers>
            <bean class="com.limyel.shutbb.resolver.CurrentUserResolver">
                <property name="authorizationUtil" ref="authorizationUtil"/>
                <property name="userDao" ref="userDao"/>
            </bean>
            <bean class="com.limyel.shutbb.resolver.PageSizeResolver"/>
            <bean class="com.limyel.shutbb.resolver.JsonItemResolver"/>
        </mvc:argument-resolvers>
    </mvc:annotation-driven>

    <context:component-scan base-package="com.limyel.shutbb.controller"/>

    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.limyel.shutbb.interceptor.CORSInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.limyel.shutbb.interceptor.AuthorizetionInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!--  文件解析  -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="20917520"/>   <!--  20m  -->
        <property name="maxInMemorySize" value="8192"/>
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="uploadTempDir" value="fileUpload/temp"/>
    </bean>
</beans>